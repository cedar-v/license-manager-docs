# License Manager Docs

简洁的 VitePress 文档站点，记录 License Manager 的使用指南与 API 说明。

## 本地快速启动

1. 安装依赖（首次运行需要）  
   `npm install`
2. 启动文档服务器（默认端口 5173）  
   `npm run docs:dev`
3. 打开浏览器访问 `http://localhost:5173` 预览。

> 如需构建静态站点，可执行 `npm run docs:build`，产物位于 `docs/.vitepress/dist`。